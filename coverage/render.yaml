# Exported from Render on 2025-09-01T19:38:54Z
services:
- type: web
  name: project-dev-resume
  runtime: node
  repo: https://github.com/Hawk-PDX/project-dev-resume
  plan: free
  envVars:
  - key: VITE_API_URL
    sync: false
  region: oregon
  buildCommand: npm install; npm run build; pip install -r backend/requirements.txt
  startCommand: cd backend && gunicorn run:app -b 0.0.0.0:$PORT --timeout 60 --workers
    2
  autoDeployTrigger: commit
version: "1"
